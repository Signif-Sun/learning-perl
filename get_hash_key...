#operate hash，practice to get keys or keys-values and so on

use strict;


#-----------------粗箭头=>来创建哈希列表--------------------------------------------------------------
my %hash_grade = ('john' => qw/sun xiao hui/,'nash' => 90,'sinmo' => 93);
#-----------------获取哈希的所有键的q值---------------------------------------------------------------
my @key_arr = keys %hash_grade;    #sort keys %hash_grade;

#print "@key_arr";
#-----------------获取单个键对应的值，使用$符号-------------------------------------------------------
#print $hash_grade{'john'};

while(my($key,$value) = each %ENV)
{
#-----------------完成字符串的内插---------------------------------------------------------------------
    #print "$key : $value\n";
}

@arr =$hash_grade{'john'};

#print $arr[0];       #不支持value是数组，只是得到数组的第一个元素而已

#-----------------字符串中只能得到某个键对应的值而不能直接获取哈希列表------------------------------------
print "the hash is $hash_grade{'john'}!";

